server.port=8082
spring.application.name=audit-service

# RabbitMQ Configuration
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# RabbitMQ custom properties
rabbitmq.queue.audit=audit.queue
rabbitmq.exchange.audit=audit.exchange
rabbitmq.routingkey.audit=audit.routing.key

# Expose all actuator endpoints for K8s liveness/readiness probes
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
